- name : Install nginx and Starting
  hosts : frontend
  become : yes
  tasks :
    - name : installing nginx
      ansible.builtin.package :
      name : nginx
      state : present

    - name : start nginx
      service :
        name : nginx
        enabled : yes
