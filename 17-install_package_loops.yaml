- name : Installing packages
  hosts : frontend
  become : true
  
  tasks :
  - name : Installing packages
    packages :
      name : "{{ item }}"
      state : present
    loop :
    - mysql
    - mongodb-org
    - redis