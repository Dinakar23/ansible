- name : Installing packages
  hosts : frontend
  become : yes
  
  tasks :
  - name : Installing packages
    packages :
      name : "{{ item }}"
      state : present
    loop :
    - mysql
    - mongodb-org
    - redis