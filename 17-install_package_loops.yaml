- name : Installing packages
  hosts : frontend
  become : yes
  
  tasks :
  - name : Installing packages
    package :
      name : "{{ item }}"
      state : present
    loop :
    - mysql
    - mongodb-org
    - redis