- name : Starting Practice on variale
  hosts : localhost
  connections : local # it will not ask for username and password
  vars :
    COURSE : "Ansible"
    TRAINER : "Siva"
  tasks :
    - debug : 
        msg : "Hi I'm learning {{ COURSE }}, and the trainer is {{ TRAINER }}"
